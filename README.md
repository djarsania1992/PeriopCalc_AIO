# PeriopCalc_AIO
Combines the various perioperative calculators used in HIM Consult notes into one single interface with outputs for each risk score or calculator.

Open `index.html` in a browser or host the folder on GitHub Pages for a static, responsive perioperative risk calculator.
